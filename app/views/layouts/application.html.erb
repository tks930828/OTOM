<!DOCTYPE html>
<html>
  <head>
    <title>otom</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
  </head>

  <body>
    <% if user_signed_in? %>
      <header>
        <div>
          <%= link_to root_path, class: "nav-link" do %>
            <i class="fas fa-book"></i> Otom
          <% end %>
        </div>
        <div><%= link_to "ホーム", root_path, class: "nav-link" %></div>
        <div><%= link_to "新規作成", new_output_path, class: "nav-link" %></div>
        <div><%= link_to "アウトプット", outputs_path, class: "nav-link" %></div>
        <div><%= link_to "カテゴリー", categories_path, class: "nav-link" %></div>
        <div><%= link_to "マイページ", user_path(current_user.id), class: "nav-link" %></div>
        <div><%= link_to "ユーザー", users_path, class: "nav-link" %></div>
        <div><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %></div>
      </header>
    <% end %>
    
    <main class="container">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="footer mt-auto py-3 bg-light">
      <div class="container text-center">
        <span class="text-muted">© 2024 Otom. All rights reserved.</span>
      </div>
    </footer>
  </body>
</html>
